import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Components/Overview" />

# Component Library

The marketing site is built on the **vendored KDS components** in `src/components/*` — the same atoms
the product ships. This library **documents** them (and the bespoke marketing organisms built from
them); it does not re-port or duplicate anything.

## Adapt, don't fork (the rule)

The vendored atom is the **single source of truth**. A "marketing version" means the atom **adapted
via tokens/props or a thin variant** — shared interaction/identity, marketing-tunable surface — like
the CTA Button (locked interaction, marketing sizing). It **never** means a duplicated component that
drifts. If an atom needs a genuine *structural* marketing adaptation, we flag it and decide
deliberately (see "Flagged" below) rather than copy-paste.

## The tree

**Atoms** — primitive, mostly self-contained.

| Component | Live page uses | Status |
|---|---|---|
| Badge | ~22 | ✅ documented |
| Avatar | ~12 | ✅ documented |
| Chip | ~3 | ✅ documented |
| IconButton | ~3 | ✅ documented |
| MessageBubble | ~3 | ✅ documented (ClientOnly) |
| Eyebrow | ~1 | ✅ documented (Primitives) |
| Button (CTA) | n/a (kaya/) | ✅ documented (Primitives) |
| DeltaPill | via StatCard | ✅ documented |
| Divider · ChipButton | sub-component | ✅ documented (general primitives) |

**Molecules** — composed of atoms.

| Component | Live page uses | Status |
|---|---|---|
| StatCard (Eyebrow + DeltaPill) | ~3 | ✅ documented |
| SectionHeading · ShowcaseFrame · FinalCTABand | n/a | ✅ documented (Primitives) |
| Tabs / TabItem | sub-component | ✅ documented (general primitive) |
| Tooltip | sub-component | ✅ documented (general primitive) |

**Organisms** — the bespoke marketing concept-visuals. Each is assembled **entirely from the
documented atoms** (Avatar, Badge, MessageBubble, StatCard) inside a ShowcaseFrame — verified by
grepping their imports. Self-contained (no props).

| Organism | File | Built from |
|---|---|---|
| PersonaDashboard | `AIAgentsPage/PersonaDashboard.tsx` | Avatar, Badge |
| ChatPinVisual | `ChatspacePage/ChatPinVisual.tsx` | Badge, MessageBubble |
| CommandGrid | `SlackPage/CommandGrid.tsx` | sections, ui |
| OrchestrationMap | `BrainPage/visuals.tsx` | Avatar, Badge, MessageBubble |
| SlackWorkforceMap | `SlackPage/visuals.tsx` | Avatar, Badge |
| UsageVisual | `AIAgentsPage/visuals.tsx` | StatCard |
| _more per-page exports_ | `*/visuals.tsx` | (same atom set) |
| Section archetypes | see **Archetypes** | tokens + primitives |

> Note: the kickoff named PinsBoard / AgentRosterGrid / CrewVisual / OrchestrationMap — only
> **OrchestrationMap** exists (a `BrainPage/visuals.tsx` export). The others don't exist under those
> names; the table above documents the organisms that **actually exist**.

## Available, not yet surfaced (deferred)

Vendored and importable, but **no stories** — grepping live imports shows **zero marketing usage**, so
documenting them would be over-building (the rule: prioritize by real usage, defer unused plumbing):

- **Product-app plumbing:** `UsageBarChart` · `StreamingIndicator` · `ChatInput` · `Checkbox` ·
  `InputField` · `Dropdown` / `DropdownMenuItem` / `DropdownSection` · `Popover` · `Switch`.
- **Model-picker family** (0 marketing-page uses; the real organisms don't import them):
  `ModelSelector` · `ModelSelectItem` · `ModelFeaturedCard`. The kickoff listed these as
  "concept-visual ingredients," but the organisms are built from Avatar/Badge/MessageBubble/StatCard
  instead — surface only if a future marketing visual actually needs the model picker.
- **`Sparkline`** — 0 uses anywhere. Queued only if a metrics visual needs an inline trend line.

## Flagged for deliberate adaptation

- **Button → marketing `lg` size:** a larger, more generous CTA size is wanted for marketing. Deferred
  — `kaya/*` is read-only; the right move is an **additive geometry-only size** upstreamed into KDS,
  not a fork. Pending sign-off (see Primitives → Button).
- _(none others yet — add here when an atom can't be adapted by tokens/props alone.)_

## Each story carries

Variants + states + a one-line **marketing-usage note**. Tokens only — no magic numbers. Product
surfaces (MessageBubble, StatCard) use the **inherited KDS type ramp**; marketing chrome uses the
marketing tier (see Foundations → Typography).
